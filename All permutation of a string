int main()
{
    string s;
    cin>>s;
    vector<string>v;
    for(int i=0;i<s.length();i++)
    {
        for(int j=1;j<=s.length()-i;j++)
        {
           v.push_back(s.substr(i,j));
        }
    }
    for(int i=0;i<v.size();i++)
    {

        cout<<v[i]<<endl;
    }
}
